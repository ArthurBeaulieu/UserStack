<div>
  <p id="error-output"></p>
</div>
<label for="lock-registration">Lock registration</label>
<input id="lock-registration" type="checkbox" {{#if locked}}checked{{/if}}/>
<h1>Registered users</h1>
<ul id="users-list">
  {{#each users}}
    <li data-id="{{this.id}}">
      <p>Username : {{username}}</p>
      <p>Email : <a href="mailto:{{this.email}}">{{this.email}}</a></p>
      <img src="{{this.avatar}}" alt="user-avatar">
      <p>Registration : {{this.registration}}</p>
      <p>Last login : {{this.lastLogin}}</p>
      <p>Godfather : {{this.godfather}}</p>
      <p>Depth from Root account : {{this.depth}}</p>
      <p>Children : {{#each this.children}}
        <span>{{this}}</span>
      {{/each}}</p>
      <p class="user-roles">Roles : {{#each this.roles}}
        <span>
          <label for="role{{this.id}}">{{this.name}}</label>
          <input id="role{{this.id}}" type="checkbox" data-id="{{this.id}}" {{#if this.checked}}checked{{/if}}/>
        </span>
      {{/each}}</p>
      <button class="delete-user">Delete</button>
    </li>
  {{/each}}
</ul>
<a href="/admin">Admin menu</a>
